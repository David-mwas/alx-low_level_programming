#include <stdio.h>
/**
 *main-Entry point prints alpha in lowercase
 *then in uppercase
 *
 *Return:Always 0 (success)
 */
int main(void)
{
char ltr;

for (ltr = a ; ltr <= z ; ltr++)
{
putchar(ltr);
}
for (ltr = A ; ltr <= Z ; ltr++)
{
putchar(ltr);
}
putchar(n);
return (0);
}
