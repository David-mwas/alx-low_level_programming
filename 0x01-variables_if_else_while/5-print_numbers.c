#include <stdio.h>
/**
 *main-Entry point prints single digit base 10
 *
 *Return:Always 0 (success)
 */
int main(void)
{
int numb;

for (numb = 0 ; numb < 10 ; numb++)
{
printf("%d", numb);
}
putchar(n);
return (0);
}
